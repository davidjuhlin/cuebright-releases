# Cuebright releases

Downloadable builds for Cuebright. No source code lives here — see the
main project repo for that. `policy.json` is what the app itself checks
on launch (and periodically) to know the latest version and the oldest
version still allowed to run.
